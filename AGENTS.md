# 项目协作与部署约定

## 开发与部署环境

- 本项目在本地环境中进行开发、调试和各类代码修改。
- 生产或运行环境部署在 Linux 服务器上。
- Linux 服务器无法通过 SSH 从本地直接连接，因此本地与 Linux 服务器之间的代码同步统一通过 Git 远程仓库中转。

## 修改后的 Git 工作流

- 每次完成代码或配置修改后，必须先检查本次变更并进行必要的验证。
- 验证完成后，必须将本次任务涉及的文件提交到 Git；不要把与当前任务无关的已有修改混入提交。
- 提交信息应简洁、准确地描述本次修改。
- 提交后应将对应分支推送到已配置的 Git 远程仓库，确保 Linux 服务器能够获取该提交。
- 除非用户明确指定其他分支，否则沿用当前工作分支及其上游远程分支。
- 不得使用会破坏用户现有工作区或提交历史的 Git 命令，例如 `git reset --hard`、强制推送或擅自覆盖已有修改。

## 任务交付要求

- 每次完成修改后，向用户说明提交哈希、提交信息和已推送的远程分支。
- 每次都必须给出可在 Linux 服务器上直接执行的代码拉取命令，并使用本次实际推送的分支名。
- 默认拉取命令格式如下；如果部署目录或分支不同，应按实际情况调整：

  ```bash
  cd <项目在 Linux 服务器上的目录>
  git pull --ff-only origin <分支名>
  ```

- 如果修改还需要执行依赖安装、数据库迁移、构建、重启服务或其他部署步骤，也应在拉取命令后按实际顺序一并列出。
- 如果因为权限、网络、远程仓库配置或其他原因无法提交或推送，必须明确说明阻塞原因，不得声称 Linux 服务器已经可以拉取最新代码。
